# 스킨 교체 기능 (Addressables) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 플레이어가 카드 스킨(앞 52장 + 뒷면을 묶은 `CardSpriteSet`)을 로비/인게임에서 선택·즉시 교체하고, 선택이 영속되며, 리소스는 Addressables 키로 로드되는 기능을 추가한다.

**Architecture:** App 싱글톤 `ISkinService`가 현재 스킨 상태·선택·영속을 소유한다. Addressables 로드/해제는 `ISkinAssetGateway` 뒤로 격리한다(기존 Auth·Stats Gateway 패턴). 스킨 목록은 `ISkinCatalog`(SO 구현 `SkinCatalogAsset`)가 메타데이터+Addressable 키로 제공한다. 인게임은 `CurrentSpriteSet` Observable을 구독해 live re-skin 한다. **로드 실패는 예외를 전파(failover 없음).**

**Tech Stack:** Unity, VContainer(DI), R3(`ReactiveProperty`/`ReadOnlyReactiveProperty`), UniTask, Unity Addressables 2.4.1, NUnit(EditMode), `uloop` CLI.

**기준 정보 (조사 결과 — 그대로 사용):**
- asmdef GUID: Model=`5f49831981d53e243b3bdefc2f57dd15`, Data=`26845efb0a4c32b41805c378a317b1c3`, Core=`668f97445995c0344b77994edc2b018c`, Gateway=`ed7b289127fa01f4aa3ce2696d0e0506`, Service=`c2884e981ff3f174e8d34a4031fd112d`, Component=`498379050a8343c469c361ec182f105d`, Scene=`722cf409e817fcc43b08f9757822a914`
- Addressables GUID: Unity.Addressables=`9e24947de15b9834991c9d8411ea37cf`, Unity.ResourceManager=`84651a3751eca9349aac36a66bba901b`, UniTask.Addressables=`593a5b492d29ac6448b1ebf7f035ef33`
- `UICard.SetSpriteSet(CardSpriteSet)` 이미 존재(UICard.cs:101) — 호출만 하면 됨.
- `Tests.EditMode.asmdef`는 이미 Model·Data·Gateway·Service·Scene·UniTask·R3 참조. **Unity.Addressables만 추가 필요.**
- `Service.asmdef`는 이미 ResourceManager·UniTask.Addressables 참조. **Data·Unity.Addressables 추가 필요.**

---

## File Structure

**신규 파일**
- `Assets/Scripts/Model/Skin/SkinId.cs` — 스킨 식별자(순수 도메인, immutable struct)
- `Assets/Scripts/Data/Skin/CardSpriteSetReference.cs` — `AssetReferenceT<CardSpriteSet>` 타입 서브클래스
- `Assets/Scripts/Data/Skin/SkinInfo.cs` — 선택 UI용 경량 메타(immutable, Sprite 포함 → Data 레이어)
- `Assets/Scripts/Data/Skin/SkinCatalogEntry.cs` — `[Serializable]` 카탈로그 항목
- `Assets/Scripts/Data/Skin/SkinCatalogLookup.cs` — 순수 lookup(중복 검출·순서·조회). `CardSpriteLookup` 미러
- `Assets/Scripts/Data/Skin/ISkinCatalog.cs` — 카탈로그 인터페이스
- `Assets/Scripts/Data/Skin/SkinCatalogAsset.cs` — `ScriptableObject` 구현
- `Assets/Scripts/Gateway/Skin/ISkinAssetGateway.cs`
- `Assets/Scripts/Gateway/Skin/AddressableSkinAssetGateway.cs`
- `Assets/Scripts/Service/SkinService/ISkinPreferenceStore.cs`
- `Assets/Scripts/Service/SkinService/PlayerPrefsSkinPreferenceStore.cs`
- `Assets/Scripts/Service/SkinService/ISkinService.cs`
- `Assets/Scripts/Service/SkinService/SkinService.cs`
- `Assets/Scripts/Scene/Lobby/View/SkinSelectView.cs`
- `Assets/Scripts/Scene/Lobby/View/SkinTileView.cs`
- `Assets/Tests/EditMode/SkinIdTests.cs`
- `Assets/Tests/EditMode/SkinCatalogLookupTests.cs`
- `Assets/Tests/EditMode/SkinServiceTests.cs`
- `Assets/Tests/EditMode/FakeSkinCatalog.cs`
- `Assets/Tests/EditMode/FakeSkinAssetGateway.cs`
- `Assets/Tests/EditMode/InMemorySkinPreferenceStore.cs`

**수정 파일**
- `Assets/Scripts/Data/Data.asmdef` — Unity.Addressables 참조 추가
- `Assets/Scripts/Gateway/Gateway.asmdef` — Data·Addressables 3종 참조 추가
- `Assets/Scripts/Service/Service.asmdef` — Data·Unity.Addressables 참조 추가
- `Assets/Tests/EditMode/Tests.EditMode.asmdef` — Unity.Addressables 참조 추가
- `Assets/Scripts/App/AppLifetimeScope.cs` — 카탈로그/게이트웨이/프리퍼런스/서비스 등록 + `[SerializeField]`
- `Assets/Scripts/App/AppPresenter.cs` — `SkinService.InitializeAsync()` 호출
- `Assets/Scripts/Component/Card/UICardsController.cs` — `ApplySpriteSet` + spawn 시 적용
- `Assets/Scripts/Scene/Ingame/IngameComponent.cs` — `ApplySpriteSet` 전달
- `Assets/Scripts/Scene/Ingame/IngamePresenter.cs` — `CurrentSpriteSet` 구독
- `Assets/Scripts/Scene/Lobby/LobbyComponent.cs` — `SkinSelectView` 노출
- `Assets/Scripts/Scene/Lobby/LobbyPresenter.cs` — 스킨 선택 배선

**Editor 작업 (코드 아님)**
- Addressables "Skins" 그룹 생성, `CardSpriteSet` 에셋 마킹(label `skin`)
- `SkinCatalog.asset` 생성·항목 입력
- App 씬/프리팹의 `AppLifetimeScope`에 `skinCatalog` 바인딩
- Lobby/Ingame 씬에 스킨 선택 UI(`SkinTile` base prefab → variant) 배치·바인딩

---

## Task 1: Model — SkinId

**Files:**
- Create: `Assets/Scripts/Model/Skin/SkinId.cs`
- Test: `Assets/Tests/EditMode/SkinIdTests.cs`

- [ ] **Step 1: Write the failing test**

Create `Assets/Tests/EditMode/SkinIdTests.cs`:

```csharp
using Model.Skin;
using NUnit.Framework;

namespace Tests.EditMode
{
    [TestFixture]
    public class SkinIdTests
    {
        [Test]
        public void Equals_SameValue_AreEqualWithSameHash()
        {
            Assert.AreEqual(new SkinId("classic"), new SkinId("classic"));
            Assert.AreEqual(new SkinId("classic").GetHashCode(), new SkinId("classic").GetHashCode());
        }

        [Test]
        public void Equals_DifferentValue_AreNotEqual()
        {
            Assert.AreNotEqual(new SkinId("classic"), new SkinId("dark"));
        }

        [Test]
        public void IsEmpty_TrueForNullOrEmpty()
        {
            Assert.IsTrue(new SkinId(null).IsEmpty);
            Assert.IsTrue(new SkinId("").IsEmpty);
            Assert.IsFalse(new SkinId("classic").IsEmpty);
        }
    }
}
```

- [ ] **Step 2: Run tests to verify the new test fails**

Run: `uloop run-tests --mode EditMode`
Expected: Compile error / FAIL — `SkinId` 타입이 없음 (`Model.Skin` 네임스페이스 미존재).

- [ ] **Step 3: Write the implementation**

Create `Assets/Scripts/Model/Skin/SkinId.cs`:

```csharp
using System;

namespace Model.Skin
{
    /// <summary>Stable identifier for a card skin. Wraps a string for type safety.</summary>
    public readonly struct SkinId : IEquatable<SkinId>
    {
        public string Value { get; }

        public SkinId(string value)
        {
            Value = value;
        }

        public bool IsEmpty => string.IsNullOrEmpty(Value);

        public bool Equals(SkinId other) => string.Equals(Value, other.Value, StringComparison.Ordinal);
        public override bool Equals(object obj) => obj is SkinId other && Equals(other);
        public override int GetHashCode() => Value == null ? 0 : StringComparer.Ordinal.GetHashCode(Value);
        public override string ToString() => Value ?? string.Empty;
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `uloop run-tests --mode EditMode`
Expected: `SkinIdTests` 3개 PASS, 기존 테스트 모두 PASS.

- [ ] **Step 5: Commit**

```bash
git add Assets/Scripts/Model/Skin/SkinId.cs Assets/Tests/EditMode/SkinIdTests.cs
git commit -m "feat(skin): add SkinId domain model"
```

---

## Task 2: Data asmdef + value types (CardSpriteSetReference, SkinInfo, SkinCatalogEntry)

**Files:**
- Modify: `Assets/Scripts/Data/Data.asmdef`
- Modify: `Assets/Tests/EditMode/Tests.EditMode.asmdef`
- Create: `Assets/Scripts/Data/Skin/CardSpriteSetReference.cs`
- Create: `Assets/Scripts/Data/Skin/SkinInfo.cs`
- Create: `Assets/Scripts/Data/Skin/SkinCatalogEntry.cs`

> 이 타입들은 데이터 홀더라 단위 테스트 대신 컴파일로 검증한다. `SkinCatalogEntry`의 public 생성자는 Task 3/5의 테스트에서 사용한다.

- [ ] **Step 1: Add Unity.Addressables reference to Data.asmdef**

`Assets/Scripts/Data/Data.asmdef`의 `references` 배열에 Addressables GUID를 추가한다. 전체 파일을 다음으로 교체:

```json
{
    "name": "Data",
    "rootNamespace": "",
    "references": [
        "GUID:c2884e981ff3f174e8d34a4031fd112d",
        "GUID:5f49831981d53e243b3bdefc2f57dd15",
        "GUID:eec0964c48f6f4e40bc3ec2257ccf8c5",
        "GUID:9e24947de15b9834991c9d8411ea37cf"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
```

- [ ] **Step 2: Add Unity.Addressables reference to Tests.EditMode.asmdef**

`Assets/Tests/EditMode/Tests.EditMode.asmdef`의 `references` 배열 끝에 `"GUID:9e24947de15b9834991c9d8411ea37cf"` 한 줄을 추가한다(다른 항목은 그대로). 결과 `references`:

```json
    "references": [
        "GUID:27619889b8ba8c24980f49ee34dbb44a",
        "GUID:0acc523941302664db1f4e527237feb3",
        "GUID:5f49831981d53e243b3bdefc2f57dd15",
        "GUID:c2884e981ff3f174e8d34a4031fd112d",
        "GUID:ed7b289127fa01f4aa3ce2696d0e0506",
        "GUID:f51ebe6a0ceec4240a699833d6309b23",
        "GUID:722cf409e817fcc43b08f9757822a914",
        "GUID:77221876cc6b8244180b96e320b1bcd4",
        "GUID:26845efb0a4c32b41805c378a317b1c3",
        "GUID:eec0964c48f6f4e40bc3ec2257ccf8c5",
        "GUID:6055be8ebefd69e48b49212b09b47b2f",
        "GUID:9e24947de15b9834991c9d8411ea37cf"
    ],
```

- [ ] **Step 3: Create CardSpriteSetReference**

Create `Assets/Scripts/Data/Skin/CardSpriteSetReference.cs`:

```csharp
using System;
using Data.Card;
using UnityEngine.AddressableAssets;

namespace Data.Skin
{
    /// <summary>
    /// Typed Addressable reference to a <see cref="CardSpriteSet"/>. Concrete subclass so the
    /// Inspector draws a type-filtered object picker (raw AssetReferenceT&lt;T&gt; does not).
    /// </summary>
    [Serializable]
    public class CardSpriteSetReference : AssetReferenceT<CardSpriteSet>
    {
        public CardSpriteSetReference(string guid) : base(guid) { }
    }
}
```

- [ ] **Step 4: Create SkinInfo**

Create `Assets/Scripts/Data/Skin/SkinInfo.cs`:

```csharp
using System;
using Model.Skin;
using UnityEngine;

namespace Data.Skin
{
    /// <summary>Lightweight immutable skin metadata for selection UI. The heavy CardSpriteSet is loaded separately.</summary>
    public sealed class SkinInfo : IEquatable<SkinInfo>
    {
        public SkinId Id { get; }
        public string DisplayNameKey { get; }
        public Sprite Thumbnail { get; }

        public SkinInfo(SkinId id, string displayNameKey, Sprite thumbnail)
        {
            Id = id;
            DisplayNameKey = displayNameKey;
            Thumbnail = thumbnail;
        }

        public bool Equals(SkinInfo other)
        {
            if (other is null) return false;
            return Id.Equals(other.Id)
                && string.Equals(DisplayNameKey, other.DisplayNameKey, StringComparison.Ordinal)
                && ReferenceEquals(Thumbnail, other.Thumbnail);
        }

        public override bool Equals(object obj) => obj is SkinInfo other && Equals(other);
        public override int GetHashCode() => Id.GetHashCode();
    }
}
```

- [ ] **Step 5: Create SkinCatalogEntry**

Create `Assets/Scripts/Data/Skin/SkinCatalogEntry.cs`:

```csharp
using System;
using Model.Skin;
using UnityEngine;

namespace Data.Skin
{
    /// <summary>One skin's serialized metadata + Addressable reference. Inspector-bindable; public ctor for tests.</summary>
    [Serializable]
    public class SkinCatalogEntry
    {
        [SerializeField] private string id;
        [SerializeField] private string displayNameKey;
        [SerializeField] private Sprite thumbnail;
        [SerializeField] private CardSpriteSetReference spriteSetRef;

        public SkinCatalogEntry() { }

        public SkinCatalogEntry(string id, string displayNameKey, Sprite thumbnail, CardSpriteSetReference spriteSetRef)
        {
            this.id = id;
            this.displayNameKey = displayNameKey;
            this.thumbnail = thumbnail;
            this.spriteSetRef = spriteSetRef;
        }

        public SkinId Id => new SkinId(id);
        public string DisplayNameKey => displayNameKey;
        public Sprite Thumbnail => thumbnail;
        public CardSpriteSetReference SpriteSetRef => spriteSetRef;

        public SkinInfo ToInfo() => new SkinInfo(Id, displayNameKey, thumbnail);
    }
}
```

- [ ] **Step 6: Verify compilation**

Run: `uloop compile`
Expected: `ErrorCount: 0`. (이 단계에서는 새 타입을 쓰는 코드가 없어 컴파일만 확인.)

- [ ] **Step 7: Commit**

```bash
git add Assets/Scripts/Data/Data.asmdef Assets/Tests/EditMode/Tests.EditMode.asmdef Assets/Scripts/Data/Skin/CardSpriteSetReference.cs Assets/Scripts/Data/Skin/SkinInfo.cs Assets/Scripts/Data/Skin/SkinCatalogEntry.cs
git commit -m "feat(skin): add Data skin value types + Addressables asmdef refs"
```

---

## Task 3: Data — SkinCatalogLookup + ISkinCatalog + SkinCatalogAsset

**Files:**
- Create: `Assets/Scripts/Data/Skin/SkinCatalogLookup.cs`
- Create: `Assets/Scripts/Data/Skin/ISkinCatalog.cs`
- Create: `Assets/Scripts/Data/Skin/SkinCatalogAsset.cs`
- Test: `Assets/Tests/EditMode/SkinCatalogLookupTests.cs`

- [ ] **Step 1: Write the failing test**

Create `Assets/Tests/EditMode/SkinCatalogLookupTests.cs`:

```csharp
using System.Collections.Generic;
using System.Text.RegularExpressions;
using Data.Skin;
using Model.Skin;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

namespace Tests.EditMode
{
    [TestFixture]
    public class SkinCatalogLookupTests
    {
        private static SkinCatalogEntry Entry(string id)
            => new SkinCatalogEntry(id, $"skin.{id}", null, new CardSpriteSetReference($"{id}-guid"));

        [Test]
        public void TryGet_ReturnsEntry_ForKnownId()
        {
            var lookup = new SkinCatalogLookup(new List<SkinCatalogEntry> { Entry("classic"), Entry("dark") });
            Assert.IsTrue(lookup.TryGet(new SkinId("dark"), out var entry));
            Assert.AreEqual("dark", entry.Id.Value);
        }

        [Test]
        public void TryGet_ReturnsFalse_ForUnknownId()
        {
            var lookup = new SkinCatalogLookup(new List<SkinCatalogEntry> { Entry("classic") });
            Assert.IsFalse(lookup.TryGet(new SkinId("ghost"), out _));
        }

        [Test]
        public void Contains_MatchesTryGet()
        {
            var lookup = new SkinCatalogLookup(new List<SkinCatalogEntry> { Entry("classic") });
            Assert.IsTrue(lookup.Contains(new SkinId("classic")));
            Assert.IsFalse(lookup.Contains(new SkinId("dark")));
        }

        [Test]
        public void Skins_PreservesOrder()
        {
            var lookup = new SkinCatalogLookup(new List<SkinCatalogEntry> { Entry("classic"), Entry("dark"), Entry("neon") });
            Assert.AreEqual(3, lookup.Skins.Count);
            Assert.AreEqual("classic", lookup.Skins[0].Id.Value);
            Assert.AreEqual("neon", lookup.Skins[2].Id.Value);
        }

        [Test]
        public void DuplicateId_FirstWins_AndWarns()
        {
            LogAssert.Expect(LogType.Warning, new Regex("Duplicate skin id"));
            var lookup = new SkinCatalogLookup(new List<SkinCatalogEntry> { Entry("classic"), Entry("classic") });
            Assert.AreEqual(1, lookup.Skins.Count);
        }
    }
}
```

- [ ] **Step 2: Run tests to verify the new test fails**

Run: `uloop run-tests --mode EditMode`
Expected: Compile error / FAIL — `SkinCatalogLookup` 타입 없음.

- [ ] **Step 3: Write SkinCatalogLookup**

Create `Assets/Scripts/Data/Skin/SkinCatalogLookup.cs`:

```csharp
using System.Collections.Generic;
using Model.Skin;
using UnityEngine;

namespace Data.Skin
{
    /// <summary>Builds id→entry and ordered SkinInfo lookups from catalog entries. Pure, testable. Mirrors CardSpriteLookup.</summary>
    public sealed class SkinCatalogLookup
    {
        private readonly List<SkinInfo> infos = new List<SkinInfo>();
        private readonly Dictionary<string, SkinCatalogEntry> byId = new Dictionary<string, SkinCatalogEntry>();

        public SkinCatalogLookup(IReadOnlyList<SkinCatalogEntry> entries)
        {
            if (entries == null) return;

            foreach (var entry in entries)
            {
                if (entry == null) continue;
                var key = entry.Id.Value ?? string.Empty;
                if (byId.ContainsKey(key))
                {
                    Debug.LogWarning($"[SkinCatalog] Duplicate skin id detected: '{key}'");
                    continue;
                }
                byId[key] = entry;
                infos.Add(entry.ToInfo());
            }
        }

        public IReadOnlyList<SkinInfo> Skins => infos;

        public bool TryGet(SkinId id, out SkinCatalogEntry entry)
            => byId.TryGetValue(id.Value ?? string.Empty, out entry);

        public bool Contains(SkinId id) => byId.ContainsKey(id.Value ?? string.Empty);
    }
}
```

- [ ] **Step 4: Write ISkinCatalog**

Create `Assets/Scripts/Data/Skin/ISkinCatalog.cs`:

```csharp
using System.Collections.Generic;
using Model.Skin;

namespace Data.Skin
{
    /// <summary>Read-only access to available skins. Implemented by SkinCatalogAsset (and test doubles).</summary>
    public interface ISkinCatalog
    {
        IReadOnlyList<SkinInfo> Skins { get; }
        bool TryGet(SkinId id, out SkinCatalogEntry entry);
        bool Contains(SkinId id);
    }
}
```

- [ ] **Step 5: Write SkinCatalogAsset**

Create `Assets/Scripts/Data/Skin/SkinCatalogAsset.cs`:

```csharp
using System.Collections.Generic;
using Model.Skin;
using UnityEngine;

namespace Data.Skin
{
    [CreateAssetMenu(fileName = "SkinCatalog", menuName = "Solitaire/Skin/Catalog")]
    public class SkinCatalogAsset : ScriptableObject, ISkinCatalog
    {
        [SerializeField] private List<SkinCatalogEntry> skins = new List<SkinCatalogEntry>();

        private SkinCatalogLookup lookup;

        public IReadOnlyList<SkinInfo> Skins
        {
            get { EnsureLookup(); return lookup.Skins; }
        }

        public bool TryGet(SkinId id, out SkinCatalogEntry entry)
        {
            EnsureLookup();
            return lookup.TryGet(id, out entry);
        }

        public bool Contains(SkinId id)
        {
            EnsureLookup();
            return lookup.Contains(id);
        }

        private void OnEnable() => Build();
        private void OnValidate() => Build();

        private void EnsureLookup()
        {
            if (lookup == null) Build();
        }

        private void Build() => lookup = new SkinCatalogLookup(skins);
    }
}
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `uloop run-tests --mode EditMode`
Expected: `SkinCatalogLookupTests` 5개 PASS, 기존 모두 PASS.

- [ ] **Step 7: Commit**

```bash
git add Assets/Scripts/Data/Skin/SkinCatalogLookup.cs Assets/Scripts/Data/Skin/ISkinCatalog.cs Assets/Scripts/Data/Skin/SkinCatalogAsset.cs Assets/Tests/EditMode/SkinCatalogLookupTests.cs
git commit -m "feat(skin): add SkinCatalog lookup, interface, and ScriptableObject"
```

---

## Task 4: Gateway — ISkinAssetGateway + AddressableSkinAssetGateway

**Files:**
- Modify: `Assets/Scripts/Gateway/Gateway.asmdef`
- Create: `Assets/Scripts/Gateway/Skin/ISkinAssetGateway.cs`
- Create: `Assets/Scripts/Gateway/Skin/AddressableSkinAssetGateway.cs`

> Addressables 런타임 의존이라 EditMode 단위 테스트 대상이 아니다(가짜 게이트웨이는 Task 5에서). 검증은 `uloop compile`.

- [ ] **Step 1: Add Data + Addressables references to Gateway.asmdef**

`Assets/Scripts/Gateway/Gateway.asmdef`의 `references`에 Data·Unity.Addressables·Unity.ResourceManager·UniTask.Addressables GUID를 추가한다. 전체 파일을 다음으로 교체:

```json
{
    "name": "Gateway",
    "rootNamespace": "",
    "references": [
        "GUID:f51ebe6a0ceec4240a699833d6309b23",
        "GUID:5f49831981d53e243b3bdefc2f57dd15",
        "GUID:756335c0388f7114790e504ed368ae1d",
        "GUID:26845efb0a4c32b41805c378a317b1c3",
        "GUID:9e24947de15b9834991c9d8411ea37cf",
        "GUID:84651a3751eca9349aac36a66bba901b",
        "GUID:593a5b492d29ac6448b1ebf7f035ef33"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": true,
    "precompiledReferences": [
        "MemoryPack.Core.dll",
        "Firebase.App.dll",
        "Firebase.Auth.dll",
        "Firebase.Platform.dll",
        "Firebase.TaskExtension.dll",
        "Firebase.Analytics.dll",
        "Firebase.Crashlytics.dll"
    ],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
```

- [ ] **Step 2: Write ISkinAssetGateway**

Create `Assets/Scripts/Gateway/Skin/ISkinAssetGateway.cs`:

```csharp
using Cysharp.Threading.Tasks;
using Data.Card;
using Data.Skin;

namespace Gateway.Skin
{
    /// <summary>Loads/releases CardSpriteSet assets via Addressables. Isolates Addressables from the service layer.</summary>
    public interface ISkinAssetGateway
    {
        /// <summary>Loads the CardSpriteSet for the reference. Throws on Addressables failure (no fallback).</summary>
        UniTask<CardSpriteSet> LoadAsync(CardSpriteSetReference reference);

        /// <summary>Releases the handle previously acquired for the reference. No-op if not currently loaded.</summary>
        void Release(CardSpriteSetReference reference);
    }
}
```

- [ ] **Step 3: Write AddressableSkinAssetGateway**

Create `Assets/Scripts/Gateway/Skin/AddressableSkinAssetGateway.cs`:

```csharp
using System.Collections.Generic;
using Cysharp.Threading.Tasks;
using Data.Card;
using Data.Skin;

namespace Gateway.Skin
{
    /// <summary>
    /// Loads CardSpriteSet assets through each entry's AssetReference using the Addressables
    /// load/release idiom (LoadAssetAsync + ReleaseAsset). Tracks which references are currently
    /// loaded to avoid the "already loaded" double-load error.
    /// </summary>
    public class AddressableSkinAssetGateway : ISkinAssetGateway
    {
        private readonly HashSet<CardSpriteSetReference> loaded = new HashSet<CardSpriteSetReference>();

        public async UniTask<CardSpriteSet> LoadAsync(CardSpriteSetReference reference)
        {
            if (loaded.Contains(reference))
                return reference.Asset as CardSpriteSet;

            var handle = reference.LoadAssetAsync<CardSpriteSet>();
            loaded.Add(reference);
            return await handle.ToUniTask();
        }

        public void Release(CardSpriteSetReference reference)
        {
            if (!loaded.Remove(reference)) return;
            reference.ReleaseAsset();
        }
    }
}
```

> 참고: `reference.LoadAssetAsync<T>()` / `reference.ReleaseAsset()`는 Addressables `AssetReference`의 표준 핸들 관리 API다. `.ToUniTask()`는 UniTask.Addressables 확장으로 제공된다. 로드 실패 시 `LoadAssetAsync`의 핸들이 예외를 던지며, 이는 그대로 전파된다(failover 없음).

- [ ] **Step 4: Verify compilation**

Run: `uloop compile`
Expected: `ErrorCount: 0`.

- [ ] **Step 5: Commit**

```bash
git add Assets/Scripts/Gateway/Gateway.asmdef Assets/Scripts/Gateway/Skin/ISkinAssetGateway.cs Assets/Scripts/Gateway/Skin/AddressableSkinAssetGateway.cs
git commit -m "feat(skin): add Addressables skin asset gateway"
```

---

## Task 5: Service — SkinService + preference store (TDD with test doubles)

**Files:**
- Modify: `Assets/Scripts/Service/Service.asmdef`
- Create: `Assets/Scripts/Service/SkinService/ISkinPreferenceStore.cs`
- Create: `Assets/Scripts/Service/SkinService/PlayerPrefsSkinPreferenceStore.cs`
- Create: `Assets/Scripts/Service/SkinService/ISkinService.cs`
- Create: `Assets/Scripts/Service/SkinService/SkinService.cs`
- Create: `Assets/Tests/EditMode/FakeSkinCatalog.cs`
- Create: `Assets/Tests/EditMode/FakeSkinAssetGateway.cs`
- Create: `Assets/Tests/EditMode/InMemorySkinPreferenceStore.cs`
- Test: `Assets/Tests/EditMode/SkinServiceTests.cs`

- [ ] **Step 1: Add Data + Unity.Addressables references to Service.asmdef**

`Assets/Scripts/Service/Service.asmdef`의 `references`에 Data·Unity.Addressables GUID를 추가한다. 전체 파일을 다음으로 교체:

```json
{
    "name": "Service",
    "rootNamespace": "",
    "references": [
        "GUID:5f49831981d53e243b3bdefc2f57dd15",
        "GUID:b0214a6008ed146ff8f122a6a9c2f6cc",
        "GUID:ed7b289127fa01f4aa3ce2696d0e0506",
        "GUID:f51ebe6a0ceec4240a699833d6309b23",
        "GUID:77221876cc6b8244180b96e320b1bcd4",
        "GUID:ee8f9324eb0e1c2499f7011010a2c1ea",
        "GUID:eec0964c48f6f4e40bc3ec2257ccf8c5",
        "GUID:84651a3751eca9349aac36a66bba901b",
        "GUID:593a5b492d29ac6448b1ebf7f035ef33",
        "GUID:26845efb0a4c32b41805c378a317b1c3",
        "GUID:9e24947de15b9834991c9d8411ea37cf"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
```

- [ ] **Step 2: Write the preference store interface + PlayerPrefs implementation**

Create `Assets/Scripts/Service/SkinService/ISkinPreferenceStore.cs`:

```csharp
using Model.Skin;

namespace Service.SkinService
{
    /// <summary>Persists the selected skin id. Abstracted over PlayerPrefs for testability.</summary>
    public interface ISkinPreferenceStore
    {
        bool TryLoad(out SkinId id);
        void Save(SkinId id);
    }
}
```

Create `Assets/Scripts/Service/SkinService/PlayerPrefsSkinPreferenceStore.cs`:

```csharp
using Model.Skin;
using UnityEngine;

namespace Service.SkinService
{
    public class PlayerPrefsSkinPreferenceStore : ISkinPreferenceStore
    {
        private const string SkinIdKey = "skin.selected_id";

        public bool TryLoad(out SkinId id)
        {
            var raw = PlayerPrefs.GetString(SkinIdKey, string.Empty);
            if (string.IsNullOrEmpty(raw))
            {
                id = default;
                return false;
            }
            id = new SkinId(raw);
            return true;
        }

        public void Save(SkinId id)
        {
            PlayerPrefs.SetString(SkinIdKey, id.Value ?? string.Empty);
            PlayerPrefs.Save();
        }
    }
}
```

- [ ] **Step 3: Write the service interface**

Create `Assets/Scripts/Service/SkinService/ISkinService.cs`:

```csharp
using System.Collections.Generic;
using Cysharp.Threading.Tasks;
using Data.Card;
using Data.Skin;
using Model.Skin;
using R3;

namespace Service.SkinService
{
    public interface ISkinService
    {
        IReadOnlyList<SkinInfo> AvailableSkins { get; }
        ReadOnlyReactiveProperty<SkinId> CurrentSkinId { get; }
        ReadOnlyReactiveProperty<CardSpriteSet> CurrentSpriteSet { get; }

        /// <summary>Restores the saved skin (or default) and loads its sprite set. Call once at app start.</summary>
        UniTask InitializeAsync();

        /// <summary>Switches to the given skin: loads new set, releases previous handle, persists selection.</summary>
        UniTask SelectSkinAsync(SkinId id);
    }
}
```

- [ ] **Step 4: Write the test doubles**

Create `Assets/Tests/EditMode/FakeSkinCatalog.cs`:

```csharp
using System.Collections.Generic;
using Data.Skin;
using Model.Skin;

namespace Tests.EditMode
{
    /// <summary>Programmatic ISkinCatalog for tests — no ScriptableObject required.</summary>
    internal class FakeSkinCatalog : ISkinCatalog
    {
        private readonly List<SkinInfo> infos = new();
        private readonly Dictionary<string, SkinCatalogEntry> byId = new();

        public FakeSkinCatalog Add(SkinCatalogEntry entry)
        {
            infos.Add(entry.ToInfo());
            byId[entry.Id.Value ?? string.Empty] = entry;
            return this;
        }

        public IReadOnlyList<SkinInfo> Skins => infos;
        public bool TryGet(SkinId id, out SkinCatalogEntry entry) => byId.TryGetValue(id.Value ?? string.Empty, out entry);
        public bool Contains(SkinId id) => byId.ContainsKey(id.Value ?? string.Empty);
    }
}
```

Create `Assets/Tests/EditMode/FakeSkinAssetGateway.cs`:

```csharp
using System.Collections.Generic;
using Cysharp.Threading.Tasks;
using Data.Card;
using Data.Skin;
using Gateway.Skin;
using UnityEngine;

namespace Tests.EditMode
{
    /// <summary>In-memory gateway: returns a distinct CardSpriteSet per reference, records Release calls.</summary>
    internal class FakeSkinAssetGateway : ISkinAssetGateway
    {
        private readonly Dictionary<CardSpriteSetReference, CardSpriteSet> assets = new();
        public readonly List<CardSpriteSetReference> Released = new();
        public int LoadCalls { get; private set; }

        private CardSpriteSet GetOrCreate(CardSpriteSetReference reference)
        {
            if (!assets.TryGetValue(reference, out var set))
            {
                set = ScriptableObject.CreateInstance<CardSpriteSet>();
                assets[reference] = set;
            }
            return set;
        }

        public UniTask<CardSpriteSet> LoadAsync(CardSpriteSetReference reference)
        {
            LoadCalls++;
            return UniTask.FromResult(GetOrCreate(reference));
        }

        public void Release(CardSpriteSetReference reference) => Released.Add(reference);
    }
}
```

Create `Assets/Tests/EditMode/InMemorySkinPreferenceStore.cs`:

```csharp
using Model.Skin;
using Service.SkinService;

namespace Tests.EditMode
{
    internal class InMemorySkinPreferenceStore : ISkinPreferenceStore
    {
        private bool hasValue;
        private SkinId value;
        public int SaveCalls { get; private set; }

        public InMemorySkinPreferenceStore() { }
        public InMemorySkinPreferenceStore(SkinId initial) { hasValue = true; value = initial; }

        public bool TryLoad(out SkinId id) { id = value; return hasValue; }
        public void Save(SkinId id) { hasValue = true; value = id; SaveCalls++; }
    }
}
```

- [ ] **Step 5: Write the failing SkinService test**

Create `Assets/Tests/EditMode/SkinServiceTests.cs`:

```csharp
using System.Text.RegularExpressions;
using Data.Skin;
using Model.Skin;
using NUnit.Framework;
using Service.SkinService;
using UnityEngine;
using UnityEngine.TestTools;

namespace Tests.EditMode
{
    [TestFixture]
    public class SkinServiceTests
    {
        private static readonly SkinId Classic = new SkinId("classic");
        private static readonly SkinId Dark = new SkinId("dark");

        private FakeSkinCatalog catalog;
        private FakeSkinAssetGateway gateway;
        private CardSpriteSetReference classicRef;
        private CardSpriteSetReference darkRef;

        [SetUp]
        public void SetUp()
        {
            classicRef = new CardSpriteSetReference("classic-guid");
            darkRef = new CardSpriteSetReference("dark-guid");
            catalog = new FakeSkinCatalog()
                .Add(new SkinCatalogEntry("classic", "skin.classic", null, classicRef))
                .Add(new SkinCatalogEntry("dark", "skin.dark", null, darkRef));
            gateway = new FakeSkinAssetGateway();
        }

        private SkinService NewService(ISkinPreferenceStore prefs) => new SkinService(catalog, gateway, prefs);

        [Test]
        public void Initialize_DefaultsToClassic_WhenNoPreference()
        {
            var svc = NewService(new InMemorySkinPreferenceStore());
            svc.InitializeAsync().GetAwaiter().GetResult();
            Assert.AreEqual(Classic, svc.CurrentSkinId.CurrentValue);
            Assert.IsNotNull(svc.CurrentSpriteSet.CurrentValue);
        }

        [Test]
        public void Initialize_RestoresSavedSkin()
        {
            var svc = NewService(new InMemorySkinPreferenceStore(Dark));
            svc.InitializeAsync().GetAwaiter().GetResult();
            Assert.AreEqual(Dark, svc.CurrentSkinId.CurrentValue);
        }

        [Test]
        public void Initialize_FallsBackToClassic_WhenSavedSkinMissingFromCatalog()
        {
            var svc = NewService(new InMemorySkinPreferenceStore(new SkinId("ghost")));
            LogAssert.Expect(LogType.Warning, new Regex("not in catalog"));
            svc.InitializeAsync().GetAwaiter().GetResult();
            Assert.AreEqual(Classic, svc.CurrentSkinId.CurrentValue);
        }

        [Test]
        public void SelectSkin_UpdatesCurrentSkinAndSpriteSet()
        {
            var svc = NewService(new InMemorySkinPreferenceStore());
            svc.InitializeAsync().GetAwaiter().GetResult();
            var classicSet = svc.CurrentSpriteSet.CurrentValue;

            svc.SelectSkinAsync(Dark).GetAwaiter().GetResult();

            Assert.AreEqual(Dark, svc.CurrentSkinId.CurrentValue);
            Assert.AreNotSame(classicSet, svc.CurrentSpriteSet.CurrentValue);
        }

        [Test]
        public void SelectSkin_ReleasesPreviousReference()
        {
            var svc = NewService(new InMemorySkinPreferenceStore());
            svc.InitializeAsync().GetAwaiter().GetResult();

            svc.SelectSkinAsync(Dark).GetAwaiter().GetResult();

            CollectionAssert.Contains(gateway.Released, classicRef);
        }

        [Test]
        public void SelectSkin_PersistsSelection()
        {
            var prefs = new InMemorySkinPreferenceStore();
            var svc = NewService(prefs);
            svc.InitializeAsync().GetAwaiter().GetResult();

            svc.SelectSkinAsync(Dark).GetAwaiter().GetResult();

            Assert.AreEqual(1, prefs.SaveCalls);
            Assert.IsTrue(prefs.TryLoad(out var saved));
            Assert.AreEqual(Dark, saved);
        }

        [Test]
        public void SelectSkin_SameId_IsNoOp()
        {
            var svc = NewService(new InMemorySkinPreferenceStore());
            svc.InitializeAsync().GetAwaiter().GetResult();
            int loadsAfterInit = gateway.LoadCalls;

            svc.SelectSkinAsync(Classic).GetAwaiter().GetResult();

            Assert.AreEqual(loadsAfterInit, gateway.LoadCalls);
            Assert.IsEmpty(gateway.Released);
        }
    }
}
```

- [ ] **Step 6: Run tests to verify the new tests fail**

Run: `uloop run-tests --mode EditMode`
Expected: Compile error / FAIL — `SkinService` 타입 없음.

- [ ] **Step 7: Write SkinService**

Create `Assets/Scripts/Service/SkinService/SkinService.cs`:

```csharp
using System.Collections.Generic;
using Cysharp.Threading.Tasks;
using Data.Card;
using Data.Skin;
using Gateway.Skin;
using Model.Skin;
using R3;
using UnityEngine;

namespace Service.SkinService
{
    public class SkinService : ISkinService
    {
        public static readonly SkinId DefaultSkinId = new SkinId("classic");

        private readonly ISkinCatalog catalog;
        private readonly ISkinAssetGateway gateway;
        private readonly ISkinPreferenceStore preferences;

        private readonly ReactiveProperty<SkinId> currentSkinId = new(default);
        private readonly ReactiveProperty<CardSpriteSet> currentSpriteSet = new(null);
        private CardSpriteSetReference currentReference;

        public SkinService(ISkinCatalog catalog, ISkinAssetGateway gateway, ISkinPreferenceStore preferences)
        {
            this.catalog = catalog;
            this.gateway = gateway;
            this.preferences = preferences;
        }

        public IReadOnlyList<SkinInfo> AvailableSkins => catalog.Skins;
        public ReadOnlyReactiveProperty<SkinId> CurrentSkinId => currentSkinId;
        public ReadOnlyReactiveProperty<CardSpriteSet> CurrentSpriteSet => currentSpriteSet;

        public async UniTask InitializeAsync()
        {
            await ApplySkinAsync(ResolveInitialSkinId(), persist: false);
        }

        public async UniTask SelectSkinAsync(SkinId id)
        {
            if (id.Equals(currentSkinId.Value) && currentSpriteSet.Value != null)
                return;
            await ApplySkinAsync(id, persist: true);
        }

        private SkinId ResolveInitialSkinId()
        {
            if (!preferences.TryLoad(out var saved))
                return DefaultSkinId;
            if (catalog.Contains(saved))
                return saved;

            // Persisted user state is a system boundary: a previously-selected skin may have been
            // removed from the catalog. Validating it here and defaulting is boundary input handling,
            // NOT a failover that masks a code bug.
            Debug.LogWarning($"[SkinService] Saved skin '{saved}' not in catalog — using default '{DefaultSkinId}'.");
            return DefaultSkinId;
        }

        private async UniTask ApplySkinAsync(SkinId id, bool persist)
        {
            if (!catalog.TryGet(id, out var entry))
                throw new KeyNotFoundException($"[SkinService] Skin id '{id}' not found in catalog.");

            // Load the new set BEFORE releasing the old one to avoid a blank frame.
            var newSet = await gateway.LoadAsync(entry.SpriteSetRef);

            var previousReference = currentReference;
            currentReference = entry.SpriteSetRef;
            if (previousReference != null && previousReference != entry.SpriteSetRef)
                gateway.Release(previousReference);

            currentSpriteSet.Value = newSet;
            currentSkinId.Value = id;

            if (persist)
                preferences.Save(id);
        }
    }
}
```

- [ ] **Step 8: Run tests to verify they pass**

Run: `uloop run-tests --mode EditMode`
Expected: `SkinServiceTests` 7개 PASS, 기존 모두 PASS.

- [ ] **Step 9: Commit**

```bash
git add Assets/Scripts/Service/Service.asmdef Assets/Scripts/Service/SkinService/ Assets/Tests/EditMode/FakeSkinCatalog.cs Assets/Tests/EditMode/FakeSkinAssetGateway.cs Assets/Tests/EditMode/InMemorySkinPreferenceStore.cs Assets/Tests/EditMode/SkinServiceTests.cs
git commit -m "feat(skin): add SkinService with selection, persistence, handle release"
```

---

## Task 6: DI wiring — AppLifetimeScope + AppPresenter init

**Files:**
- Modify: `Assets/Scripts/App/AppLifetimeScope.cs`
- Modify: `Assets/Scripts/App/AppPresenter.cs`

> 검증은 `uloop compile`. 실제 catalog 에셋 바인딩은 Task 9에서 한다(여기서는 코드 배선만).

- [ ] **Step 1: Register skin services in AppLifetimeScope**

`Assets/Scripts/App/AppLifetimeScope.cs` 수정:

(a) using 추가 (파일 상단 using 블록):
```csharp
using Data.Skin;
using Gateway.Skin;
using Service.SkinService;
```

(b) `[SerializeField]` 헤더 영역에 catalog 필드 추가 (achievementCatalog 선언 바로 아래, line 39 다음):
```csharp
        [Header("Skin")]
        [SerializeField] private SkinCatalogAsset skinCatalog;
```

(c) `Configure` 안, Achievement 등록 블록 다음(line 116 `PlatformAchievementMirror` 등록 뒤)과 Consent 등록 앞 사이에 추가:
```csharp
            // Skin — App-scoped so Lobby and Ingame share one selection/state.
            if (skinCatalog == null)
            {
                throw new System.InvalidOperationException(
                    "[AppLifetimeScope] skinCatalog reference is missing — wire SkinCatalog.asset in the App scene/prefab.");
            }
            builder.RegisterInstance(skinCatalog).AsSelf().As<ISkinCatalog>();
            builder.Register<AddressableSkinAssetGateway>(Lifetime.Singleton).As<ISkinAssetGateway>();
            builder.Register<PlayerPrefsSkinPreferenceStore>(Lifetime.Singleton).As<ISkinPreferenceStore>();
            builder.Register<SkinService>(Lifetime.Singleton).As<ISkinService>();
```

- [ ] **Step 2: Initialize skin at app start in AppPresenter**

`Assets/Scripts/App/AppPresenter.cs` 수정:

(a) using 추가:
```csharp
using Service.SkinService;
```

(b) 注入 프로퍼티 추가 (다른 `[Inject]` 프로퍼티 옆, line 16 다음):
```csharp
        [Inject] private ISkinService SkinService { get; set; }
```

(c) `Start()`에서 Localization 초기화 다음(line 24 `await LocalizationService.InitializeAsync();` 뒤)에 추가:
```csharp
            await SkinService.InitializeAsync();
```

> 순서 중요: 첫 씬(`Login`) 라우팅 전에 스킨이 로드되어 있어야 Ingame 진입 시 즉시 적용된다.

- [ ] **Step 3: Verify compilation**

Run: `uloop compile`
Expected: `ErrorCount: 0`. (런타임에서 `skinCatalog` 미바인딩 시 throw하지만, 컴파일은 통과.)

- [ ] **Step 4: Commit**

```bash
git add Assets/Scripts/App/AppLifetimeScope.cs Assets/Scripts/App/AppPresenter.cs
git commit -m "feat(skin): register skin services and initialize at app start"
```

---

## Task 7: Live re-skin — UICardsController + IngameComponent + IngamePresenter

**Files:**
- Modify: `Assets/Scripts/Component/Card/UICardsController.cs`
- Modify: `Assets/Scripts/Scene/Ingame/IngameComponent.cs`
- Modify: `Assets/Scripts/Scene/Ingame/IngamePresenter.cs`

> 검증은 `uloop compile` + (선택) Play 모드 스모크. Component asmdef는 이미 Data 참조하므로 변경 없음.

- [ ] **Step 1: Add ApplySpriteSet to UICardsController**

`Assets/Scripts/Component/Card/UICardsController.cs` 수정:

(a) using 추가 (파일 상단):
```csharp
using Data.Card;
```

(b) 필드 추가 (`cardPrefab` 직렬화 필드 근처, line 19 다음):
```csharp
        private CardSpriteSet currentSpriteSet;
```

(c) public 메서드 추가 (`SpawnCard` 위, 예: line 191 `// Todo: Pooling` 앞):
```csharp
        /// <summary>Sets the active skin and re-skins all currently spawned cards. Newly spawned cards use it too.</summary>
        public void ApplySpriteSet(CardSpriteSet spriteSet)
        {
            currentSpriteSet = spriteSet;
            if (spriteSet == null) return;
            foreach (var card in activeCards)
            {
                if (card != null) card.SetSpriteSet(spriteSet);
            }
        }
```

(d) `SpawnCard` 안에서 새 카드에 현재 스킨 적용. `uiCard.SetCard(card);` (line 199) **바로 앞**에 추가:
```csharp
            if (currentSpriteSet != null) uiCard.SetSpriteSet(currentSpriteSet);
```

> `SetSpriteSet`은 내부에서 `RefreshCardSprites`를 호출하므로, `SetCard`(역시 Refresh 호출) 전에 셋을 지정하면 한 번에 올바른 스프라이트로 렌더된다. `SetCard`가 다시 Refresh하지만 동일 셋이라 무해.

- [ ] **Step 2: Forward ApplySpriteSet through IngameComponent**

`Assets/Scripts/Scene/Ingame/IngameComponent.cs` 수정:

(a) using 추가:
```csharp
using Data.Card;
```

(b) `SpawnCard` 전달 메서드 근처(line 334 `public UICard SpawnCard(...)` 앞)에 추가:
```csharp
        public void ApplySpriteSet(CardSpriteSet spriteSet)
            => cardsController.ApplySpriteSet(spriteSet);
```

- [ ] **Step 3: Subscribe to CurrentSpriteSet in IngamePresenter**

`Assets/Scripts/Scene/Ingame/IngamePresenter.cs` 수정:

(a) using 추가:
```csharp
using Service.SkinService;
```

(b) `[Inject]` 프로퍼티 추가 (다른 inject 옆, line 55 `ShuffleStrategyProvider` 다음):
```csharp
        [Inject] private ISkinService SkinService { get; set; }
```

(c) `Start()` 안 최상단, `InitializeGameAsync().Forget();` (line 73) **앞**에 구독 추가:
```csharp
            // ReactiveProperty replays current value on subscribe, so the active skin is applied
            // before the first deal. Subsequent in-game skin changes re-skin live.
            SkinService.CurrentSpriteSet
                .Where(set => set != null)
                .Subscribe(set => Component.ApplySpriteSet(set))
                .AddTo(Component);
```

> `Component`는 `IngameComponent`(MonoBehaviour)라 `.AddTo(Component)`로 씬 파기 시 자동 해제된다(기존 구독들과 동일 패턴).

- [ ] **Step 4: Verify compilation**

Run: `uloop compile`
Expected: `ErrorCount: 0`.

- [ ] **Step 5: Commit**

```bash
git add Assets/Scripts/Component/Card/UICardsController.cs Assets/Scripts/Scene/Ingame/IngameComponent.cs Assets/Scripts/Scene/Ingame/IngamePresenter.cs
git commit -m "feat(skin): apply current skin to cards and re-skin live in-game"
```

---

## Task 8: Lobby skin selection UI (code)

**Files:**
- Create: `Assets/Scripts/Scene/Lobby/View/SkinTileView.cs`
- Create: `Assets/Scripts/Scene/Lobby/View/SkinSelectView.cs`
- Modify: `Assets/Scripts/Scene/Lobby/LobbyComponent.cs`
- Modify: `Assets/Scripts/Scene/Lobby/LobbyPresenter.cs`

> 뷰 코드만 작성한다. 실제 prefab/씬 배치·바인딩은 Task 10(Editor)에서 한다. 검증은 `uloop compile`. Scene asmdef는 이미 Service·Data 참조(전 레이어)하므로 변경 없음.

- [ ] **Step 1: Write SkinTileView (per-item view)**

Create `Assets/Scripts/Scene/Lobby/View/SkinTileView.cs`:

```csharp
using Data.Skin;
using Model.Skin;
using R3;
using UnityEngine;
using UnityEngine.UI;

namespace Scene.Lobby.View
{
    /// <summary>
    /// One selectable skin tile. Base prefab "SkinTile" → variants placed in the grid
    /// (Prefab Variant 우선 원칙, GameTileView와 동일 결).
    /// </summary>
    public class SkinTileView : MonoBehaviour
    {
        [SerializeField] private Image thumbnailImage;
        [SerializeField] private Button button;
        [SerializeField] private GameObject selectedIndicator;

        private readonly Subject<SkinId> onClickedSubject = new Subject<SkinId>();
        public Observable<SkinId> OnClickedObservable() => onClickedSubject;

        private SkinId skinId;

        private void Awake()
        {
            if (button != null) button.onClick.AddListener(() => onClickedSubject.OnNext(skinId));
        }

        public void Bind(SkinInfo info)
        {
            skinId = info.Id;
            if (thumbnailImage != null) thumbnailImage.sprite = info.Thumbnail;
        }

        public void SetSelected(bool selected)
        {
            if (selectedIndicator != null) selectedIndicator.SetActive(selected);
        }

        private void OnDestroy() => onClickedSubject.Dispose();
    }
}
```

- [ ] **Step 2: Write SkinSelectView (grid container)**

Create `Assets/Scripts/Scene/Lobby/View/SkinSelectView.cs`:

```csharp
using System.Collections.Generic;
using Data.Skin;
using Model.Skin;
using R3;
using UnityEngine;

namespace Scene.Lobby.View
{
    /// <summary>
    /// Builds a grid of SkinTileView from the available skins and surfaces selection clicks.
    /// Highlights the current skin. Token-light: thumbnails come from the catalog (already bundled).
    /// </summary>
    public class SkinSelectView : MonoBehaviour
    {
        [SerializeField] private Transform tileParent;
        [SerializeField] private SkinTileView tilePrefab;

        private readonly List<SkinTileView> tiles = new List<SkinTileView>();
        private readonly Subject<SkinId> onSkinSelectedSubject = new Subject<SkinId>();
        private readonly Dictionary<SkinTileView, SkinId> tileIds = new Dictionary<SkinTileView, SkinId>();

        public Observable<SkinId> OnSkinSelectedObservable() => onSkinSelectedSubject;

        public void Build(IReadOnlyList<SkinInfo> skins)
        {
            Clear();
            if (tilePrefab == null || tileParent == null) return;

            foreach (var info in skins)
            {
                var tile = Instantiate(tilePrefab, tileParent);
                tile.Bind(info);
                tileIds[tile] = info.Id;
                tile.OnClickedObservable()
                    .Subscribe(id => onSkinSelectedSubject.OnNext(id))
                    .AddTo(tile);
                tiles.Add(tile);
            }
        }

        public void SetSelected(SkinId currentId)
        {
            foreach (var tile in tiles)
            {
                if (tile == null) continue;
                tile.SetSelected(tileIds.TryGetValue(tile, out var id) && id.Equals(currentId));
            }
        }

        private void Clear()
        {
            foreach (var tile in tiles)
                if (tile != null) Destroy(tile.gameObject);
            tiles.Clear();
            tileIds.Clear();
        }

        private void OnDestroy()
        {
            onSkinSelectedSubject.Dispose();
        }
    }
}
```

- [ ] **Step 3: Expose SkinSelectView via LobbyComponent**

`Assets/Scripts/Scene/Lobby/LobbyComponent.cs` 수정. 다음을 클래스에 추가한다(파일을 열어 기존 패턴에 맞춰 배치):

(a) using 추가:
```csharp
using System.Collections.Generic;
using Data.Skin;
using Model.Skin;
using R3;
using Scene.Lobby.View;
```

(b) `[SerializeField]` 필드 추가:
```csharp
        [SerializeField] private SkinSelectView skinSelectView;
```

(c) public API 추가:
```csharp
        public Observable<SkinId> OnSkinSelectedObservable()
            => skinSelectView != null ? skinSelectView.OnSkinSelectedObservable() : Observable.Empty<SkinId>();

        public void BuildSkinSelect(IReadOnlyList<SkinInfo> skins) => skinSelectView?.Build(skins);

        public void SetSelectedSkin(SkinId currentId) => skinSelectView?.SetSelected(currentId);
```

- [ ] **Step 4: Wire selection in LobbyPresenter**

`Assets/Scripts/Scene/Lobby/LobbyPresenter.cs` 수정:

(a) using 추가 (필요한 것만; 기존에 있으면 생략):
```csharp
using Cysharp.Threading.Tasks;
using R3;
using Service.SkinService;
using VContainer;
```

(b) `[Inject]` 프로퍼티 추가:
```csharp
        [Inject] private ISkinService SkinService { get; set; }
```

(c) `Start()`(또는 `IStartable.Start`) 본문에 스킨 셀렉트 초기화·구독 추가. LobbyComponent 참조 프로퍼티 이름이 기존 코드에서 `Component`라고 가정(다르면 그 이름 사용):
```csharp
            Component.BuildSkinSelect(SkinService.AvailableSkins);
            Component.SetSelectedSkin(SkinService.CurrentSkinId.CurrentValue);

            SkinService.CurrentSkinId
                .Subscribe(id => Component.SetSelectedSkin(id))
                .AddTo(Component);

            Component.OnSkinSelectedObservable()
                .Subscribe(id => SkinService.SelectSkinAsync(id).Forget())
                .AddTo(Component);
```

> `LobbyPresenter`가 `LobbyComponent`를 어떤 이름으로 주입받는지 파일을 열어 확인하고 그 이름을 사용한다. `.AddTo(Component)` 대상도 동일 컴포넌트.

- [ ] **Step 5: Verify compilation**

Run: `uloop compile`
Expected: `ErrorCount: 0`.

- [ ] **Step 6: Commit**

```bash
git add Assets/Scripts/Scene/Lobby/View/SkinTileView.cs Assets/Scripts/Scene/Lobby/View/SkinSelectView.cs Assets/Scripts/Scene/Lobby/LobbyComponent.cs Assets/Scripts/Scene/Lobby/LobbyPresenter.cs
git commit -m "feat(skin): add lobby skin selection view and wiring"
```

---

## Task 9: Addressables + asset setup (Unity Editor)

> 코드가 아닌 Editor 작업. 각 단계 후 `uloop compile`로 에러 0 확인. 가능하면 Play 모드 스모크 테스트로 마무리.

- [ ] **Step 1: Mark the existing classic CardSpriteSet as Addressable**

1. 현재 `Card.prefab`의 `UICard.cardSpriteSet`이 가리키는 기존 `CardSpriteSet` 에셋을 Project 창에서 찾는다.
2. Inspector에서 **Addressable** 체크박스를 켠다. Address를 `skin/classic`으로 지정.
3. 이 에셋을 새 그룹 "Skins"에 넣는다(없으면 다음 단계에서 생성). Label `skin` 추가.

- [ ] **Step 2: Create the "Skins" Addressables group (local)**

1. `Window > Asset Management > Addressables > Groups` 열기.
2. `Create > Group > Packed Assets` → 이름 "Skins". 기본 로컬 빌드 설정(BuildPath/LoadPath = Local) 사용.
3. classic 에셋을 Skins 그룹으로 드래그. (추가 스킨 에셋도 동일 절차로 마킹+Skins 그룹+label `skin`.)

- [ ] **Step 3: Create SkinCatalog.asset**

1. Project 창에서 `Create > Solitaire > Skin > Catalog` → `Assets/.../Data/SkinCatalog.asset` 생성(기존 catalog 에셋들과 같은 폴더 규칙).
2. `skins` 리스트에 항목 추가:
   - **classic**: id=`classic`, displayNameKey=`skin.classic`(Localization 키), thumbnail=클래식 썸네일 Sprite, spriteSetRef=classic `CardSpriteSet`(드롭다운에서 선택; Addressable로 마킹돼 있어 목록에 나타남).
   - (선택) 추가 스킨 항목들.
3. `id`는 `SkinService.DefaultSkinId`("classic")과 정확히 일치해야 한다.

- [ ] **Step 4: Bind catalog to AppLifetimeScope**

1. App 씬(또는 App 부트스트랩 프리팹)에서 `AppLifetimeScope` 컴포넌트를 선택.
2. 새 "Skin > Skin Catalog" 필드에 `SkinCatalog.asset`을 드래그.

- [ ] **Step 5: Build Addressables content + verify**

1. Addressables Groups 창 → `Build > New Build > Default Build Script`.
2. Run: `uloop compile` → `ErrorCount: 0`.
3. (스모크) Play 모드 진입: Login→Lobby→Ingame 라우팅 중 콘솔에 스킨 로드 예외가 없고 카드가 classic 스프라이트로 보이는지 확인.

- [ ] **Step 6: Commit**

```bash
git add Assets
git commit -m "chore(skin): mark classic CardSpriteSet addressable, add SkinCatalog asset"
```

> `.asset`/`.meta`/Addressables 설정 변경은 바이너리/직렬화 파일이라 코드 리뷰 대신 Editor에서 검증한다. 비밀정보 없음 확인 후 커밋.

---

## Task 10: Skin selection UI placement (Unity Editor) + final verification

> 코드가 아닌 prefab/씬 작업. Prefab Variant 우선 원칙 적용.

- [ ] **Step 1: Create the SkinTile base prefab**

1. UI 요소(루트에 `Button`, 자식에 썸네일 `Image`, 선택 표시용 `GameObject`)로 `SkinTile` base prefab 생성.
2. 루트에 `SkinTileView` 컴포넌트 부착, `thumbnailImage`/`button`/`selectedIndicator` 필드를 Inspector에서 바인딩.

- [ ] **Step 2: Place SkinSelectView in Lobby**

1. Lobby 씬에 스킨 선택 패널(그리드 컨테이너) 추가. 그리드 루트에 `SkinSelectView` 부착.
2. `tileParent`=그리드 컨테이너 Transform, `tilePrefab`=`SkinTile` base prefab 바인딩.
3. `LobbyComponent`의 `skinSelectView` 필드에 이 `SkinSelectView`를 바인딩.

- [ ] **Step 3: (Ingame) reuse the same selection panel**

1. 인게임 설정 패널(`SettingPanelView` 영역)에 동일한 `SkinSelectView`를 배치하거나, 동일 base prefab으로 변형 배치.
2. 인게임 쪽 프레젠터가 로비와 동일한 패턴(`AvailableSkins` 빌드 + `OnSkinSelectedObservable`→`SelectSkinAsync` + `CurrentSkinId`→`SetSelected`)으로 배선되도록 한다. (인게임은 `SkinService.CurrentSpriteSet` 구독이 이미 카드 re-skin을 처리하므로, 선택 패널은 상태 갱신만 담당.)

- [ ] **Step 4: Full verification**

1. Run: `uloop run-tests --mode EditMode` → 전체 PASS.
2. Run: `uloop compile` → `ErrorCount: 0`.
3. Play 모드 스모크:
   - 로비에서 스킨 탭 → 선택 표시가 이동.
   - 인게임 진입 → 선택한 스킨으로 카드가 그려짐.
   - 인게임 설정에서 다른 스킨 선택 → 화면의 카드가 즉시 바뀜(live re-skin).
   - 앱 재시작 → 마지막 선택 스킨이 복원됨.

- [ ] **Step 5: Commit**

```bash
git add Assets
git commit -m "feat(skin): place skin selection UI in lobby and in-game"
```

---

## Self-Review (작성자 점검 결과)

**Spec coverage** — 스펙 각 절 대응:
- §6 Model(SkinId) → Task 1. §7 Data(Entry/Catalog/Lookup/Info/Reference) → Task 2, 3. §8 Gateway → Task 4. §9 Service(+PreferenceStore) → Task 5. §10 DI 배선 → Task 6. §11 앱 초기화 → Task 6. §12 인게임 live re-skin → Task 7. §13 로비 UI → Task 8(코드)+Task 10(배치). §14 에러 처리(no-failover + 경계 검증) → Task 5 `ApplySkinAsync`(throw) / `ResolveInitialSkinId`(경계 검증) + 테스트. §15 테스트 → Task 1/3/5. §16 Addressables 셋업 → Task 9. §17 레이어 요약 → 전체. §18 asmdef → Task 2/4/5. 누락 없음.

**Placeholder scan** — "TBD/적절히 처리" 류 없음. 모든 코드 단계에 실제 코드 포함. Editor 단계(Task 9/10)는 코드가 아닌 정당한 수작업이며 구체적 절차를 명시.

**Type consistency** — 전 태스크에서 시그니처 일치 확인: `SkinId(string)`/`.Value`/`.IsEmpty`; `SkinCatalogEntry(string,string,Sprite,CardSpriteSetReference)`/`.Id`/`.SpriteSetRef`/`.ToInfo()`; `ISkinCatalog.{Skins,TryGet,Contains}`; `ISkinAssetGateway.{LoadAsync,Release}`; `ISkinService.{AvailableSkins,CurrentSkinId,CurrentSpriteSet,InitializeAsync,SelectSkinAsync}`; `SkinService.DefaultSkinId`; R3 `ReadOnlyReactiveProperty<T>.CurrentValue`(테스트)/`ReactiveProperty<T>.Value`(구현); `UICardsController.ApplySpriteSet`↔`IngameComponent.ApplySpriteSet`↔`IngamePresenter` 구독. 불일치 없음.
